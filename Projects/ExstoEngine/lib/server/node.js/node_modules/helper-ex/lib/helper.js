var helper = exports;

helper.extend = function (object, other) {
	for(var prop in other) {
		if(object[prop] instanceof Object) {	
			helper.extend(object[prop], other[prop]);
		} else {
			object[prop] = other[prop];
		}
	}
	return object;
};