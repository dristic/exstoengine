var io = require('socket.io'),
	um = require('./usermanager');

exports.bind = function(socket) {
	// Create room
	socket.on('createRoom', function(data) {
		var username = um.getUser(socket);
		console.log('Create room: ' + data.name + ' by ' + username);
	});
	
	// Join room
};